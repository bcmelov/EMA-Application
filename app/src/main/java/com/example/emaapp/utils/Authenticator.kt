package com.example.emaapp.utils

import okhttp3.Authenticator
import okhttp3.Request
import okhttp3.Response
import okhttp3.Route

/*CLASS IS CURRENTLY NOT BEING USED*/

//class AppAuthenticator(
//    private val tokensProvider: LoginActivity.TokenRepository
//) : Authenticator {
//    override fun authenticate(route: Route?, response: Response): Request? {
//        return response.request.newBuilder().header("AccessToken", tokensProvider.token).build()
//    }
//}
